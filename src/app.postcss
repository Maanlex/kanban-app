/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

.btn{
    @apply rounded-lg;
    @apply no-animation;
}
.input{
    @apply rounded-lg;
}
.menu{
    @apply border-2;
    @apply border-base-300;
}
.modal-box{
    @apply border-2;
    @apply border-base-300;
}